<nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
    <a class="navbar-brand" href="{{ base_dir }}">
        <img src="{{ asset('build/images/icon.png') }}" height="30" alt="">
        <span class="align-middle">{{ 'base.title'|trans({}, 'base') }}</span>
    </a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
            aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            {#<li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true"
                   aria-expanded="false">
                    {% set lang_active = 'header.' ~ app.request.locale %}
                    {{ 'header.lang'|trans({}, 'resume') }}: {{ lang_active|trans({}, 'resume') }}
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('resume_page', {'_locale': 'zh'}) }}">{{ "header.zh"|trans({}, 'base') }}</a>
                    <a class="dropdown-item" href="{{ path('resume_page', {'_locale': 'en'}) }}">{{ "header.en"|trans({}, 'base') }}</a>
                    <a class="dropdown-item" href="{{ path('resume_page', {'_locale': 'fr'}) }}">{{ "header.fr"|trans({}, 'base') }}</a>
                </div>
            </li>#}
        </ul>

        <button type="button" class="btn bmd-btn-fab bmd-btn-fab-sm music"
                id="music-play"
                data-toggle="tooltip" title="{{ 'cv.music.tlp'|trans({}, 'resume') }}">
            <i class="fas fa-music"></i>
        </button>

        <div class="dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true"
               aria-expanded="false">
                {% set lang_active = 'header.' ~ app.request.locale %}

                {{ 'header.lang'|trans({}, 'base') }}&nbsp;<i class="fas fa-language fa-lg align-middle"></i>&nbsp;:&nbsp;{{ lang_active|trans({}, 'base') }}

            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <a class="dropdown-item" href="{{ path('resume_page', {'_locale': 'zh'}) }}">{{ "header.zh"|trans({}, 'base') }}</a>
                <a class="dropdown-item" href="{{ path('resume_page', {'_locale': 'en'}) }}">{{ "header.en"|trans({}, 'base') }}</a>
                <a class="dropdown-item" href="{{ path('resume_page', {'_locale': 'fr'}) }}">{{ "header.fr"|trans({}, 'base') }}</a>
            </div>
        </div>

        <audio id="music-src" controls loop preload="none" style="display:none;">
            <source src="{{ asset('files/old_street_Li_Ronghao.mp3') }}" type="audio/mpeg">
            Your browser does not support the audio element.
        </audio>
    </div>
</nav>